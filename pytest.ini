[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    live: requires AMD_VLLM_BASE_URL reachable (Slice 0.5 prereq)
    postgres: requires DATABASE_URL pointing at a running postgres
addopts = -ra --strict-markers
